@model DBL.Models.Systemstoreitemsdata
@{
    ViewData["Title"] = "Ecommerce";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    @if (Model != null && Model.Systemstoreitems != null)
    {
        foreach (var product in Model.Systemstoreitems)
        {
            <div class="col-lg-2 col-md-3 col-sm-4 col-6 mb-3">
                <a asp-action="Shopproductdetail" asp-controller="Home" asp-route-code="@Guid.NewGuid()" asp-route-Shopproductid="@product.Storeitemid" asp-area="" class="text-decoration-none">
                    <div class="card h-100 shadow-sm card-info card-outline">
                        <img src="@Url.Content(product.Storeproductimgurl)" alt="@product.Storeitemname" class="card-img-top" style="height: 140px; object-fit: cover;" />
                        <div class="card-body pt-2 pr-2 pl-2 d-flex flex-column">
                            <p class="card-text text-center text-muted text-truncate mb-1" title="@product.Storeitemname">@product.Storeitemname</p>
                            <p class="card-text text-center text-muted text-truncate mb-1" title="Kes. @product.Itemsellingprice">Kes. @product.Itemsellingprice.ToString("#,##0.00")</p>
                        </div>
                    </div>
                </a>
            </div>
        }
    }
    else
    {
        <p class="text-center">No products available at the moment.</p>
    }
</div>
<!-- Uttambsolutionsdisplayad -->
<ins class="adsbygoogle"
     style="display:block"
     data-ad-client="ca-pub-2850934153859537"
     data-ad-slot="1149686671"
     data-ad-format="auto"
     data-full-width-responsive="true"></ins>
<script>
    (adsbygoogle = window.adsbygoogle || []).push({});
</script>
<!-- Styles -->
<style>
    .card {
        transition: transform 0.2s;
        margin-bottom: 0;
    }

        .card:hover {
            transform: scale(1.05);
        }

    .card-title {
        font-size: 0.9rem;
        font-weight: bold;
    }

    .card-text {
        font-size: 0.8rem;
        color: #555;
    }

    .text-truncate {
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .text-decoration-none {
        text-decoration: none;
    }

    a:hover {
        text-decoration: none;
    }
</style>
