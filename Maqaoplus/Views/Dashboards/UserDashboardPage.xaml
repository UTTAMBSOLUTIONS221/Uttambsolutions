<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Maqaoplus.ViewModels.HouseTenant"
             x:Class="Maqaoplus.Views.Dashboards.UserDashboardPage"
             Title="Tenant Dashboard">

    <ContentPage.BindingContext>
        <local:Propertyhousetenantviewmodel />
    </ContentPage.BindingContext>

    <ScrollView>
        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Frame Grid.Row="0" Padding="10" BackgroundColor="LightGray" CornerRadius="10">
                <StackLayout>
                    <Label Text="Tenant Information" FontAttributes="Bold" FontSize="Large"/>
                    <Label Text="{Binding TenantData.Firstname}" FontSize="Medium"/>
                    <Label Text="{Binding TenantData.Lastname}" FontSize="Medium"/>
                </StackLayout>
            </Frame>

            <Frame Grid.Row="1" Padding="10" BackgroundColor="LightGray" CornerRadius="10" Margin="0,10,0,0">
                <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" Spacing="20">
                    <StackLayout>
                        <Label Text="Last Meter Reading" FontAttributes="Bold" FontSize="Medium"/>
                        <Label Text="{Binding TenantData.Tenantroomdata.Previousmeters}" FontSize="Medium"/>
                    </StackLayout>
                    <BoxView WidthRequest="1" BackgroundColor="Black" VerticalOptions="FillAndExpand"/>
                    <StackLayout>
                        <Label Text="Wallet Balance" FontAttributes="Bold" FontSize="Medium"/>
                        <Label Text="{Binding TenantData.Walletbalance}" FontSize="Medium"/>
                    </StackLayout>
                </StackLayout>
            </Frame>

            <Frame Grid.Row="2" Padding="10" BackgroundColor="LightGray" CornerRadius="10" Margin="0,10,0,0">
                <StackLayout>
                    <Label Text="Tenant Room Payments" FontAttributes="Bold" FontSize="Large"/>
                    <CollectionView ItemsSource="{Binding TenantData.Tenantroomdata.Tenantroompayments}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding Paymentamount}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Monthlyrentamount}"/>
                                    <Label Text="{Binding Datecreated, StringFormat='{}{0:MM/dd/yyyy}'}"/>
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </Frame>

            <Frame Grid.Row="3" Padding="10" BackgroundColor="LightGray" CornerRadius="10" Margin="0,10,0,0">
                <StackLayout>
                    <Label Text="Tenant Room History" FontAttributes="Bold" FontSize="Large"/>
                    <CollectionView ItemsSource="{Binding TenantData.Tenantroomdata.Tenantroomhistory}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout Orientation="Vertical">
                                    <Label Text="{Binding Propertyhousename}" FontAttributes="Bold"/>
                                    <Label Text="{Binding Systempropertyhousesizename}"/>
                                    <Label Text="{Binding Datecreated, StringFormat='{}{0:MM/dd/yyyy}'}"/>
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </Frame>
        </Grid>
    </ScrollView>
</ContentPage>
