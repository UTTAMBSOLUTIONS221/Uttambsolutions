<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maqaoplus.Views.PropertyHouse.Modal.SystemPropertyHouseAgreementModalPage"
             Title="SystemPropertyHouseAgreementModalPage">
    <ScrollView>
        <StackLayout Padding="20">
            <Label Text="Rental Management System Agreement" 
                   FontAttributes="Bold" 
                   FontSize="20" 
                   HorizontalOptions="Center" />

            <Label Text="{Binding AgreementText}" 
                   FontSize="14" 
                   Margin="0,20,0,20" />


            <!-- Navigation Button -->
            <FlexLayout Direction="Row" JustifyContent="SpaceBetween" AlignItems="Center" Margin="0,16">
                <Button Text="Cancel" Command="{Binding OnCancelClickedCommand}" BackgroundColor="Gray" TextColor="White" Padding="12" CornerRadius="5" FontSize="18" FontAttributes="Bold" HeightRequest="50" WidthRequest="150" />
                <Button Text="Next" Command="{Binding AgreePropertyHouseAgreementCommand}" BackgroundColor="#0a506c" TextColor="White" CornerRadius="5" Padding="12"  FontSize="18" HeightRequest="50"  WidthRequest="150"/>
            </FlexLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>
